# Evaluation tooling dependencies (Phase 2) — dev-time only, never a runtime dependency.
# Pinned to the Chroma server version in docker-compose (0.5.23) so the Python
# client speaks the same HTTP API generation (v1) as the server; chromadb 1.x
# clients use the v2 API and cannot talk to a 0.5.x server.
chromadb==0.5.23
# chromadb 0.5.x calls posthog's old capture() signature; posthog 6.x changed
# the API and spams "Failed to send telemetry event" warnings on every client
# creation. Pin below 6 to keep stderr clean.
posthog<6

# Step 1 dependencies (eval runner: dataset loader + API client + tests)
requests==2.32.3
pytest==8.3.3

# Step 3 dependencies — Ragas generation metrics
ragas==0.2.6
# 2.0.0 forwarded Ragas's `temperature` kwarg straight to the raw gRPC client
# (generate_content() rejects it → every judge call failed with TypeError).
# 2.0.11 consumes generation kwargs into generation_config correctly.
langchain-google-genai==2.0.11
datasets==2.18.0
python-dotenv==1.0.0
